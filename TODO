* don't mutate declaration statements?

* AbstractMutator: iteration tracking?

* Pattern ideas:
    * common-sub-expression elimination
        - improve
            -> type detection: more than BasicType
    * rewrite single array accesses in a loop with a temporary variable?
        - in most cases, the index likely depends on the loop variable...

* LoopUnrolling
    * Improve detection?
